#pragma once

//Might not need because sf already has vector2
class Vector
{
public:
    float speed;
    int direction;
    Vector()
    {
        speed = 0.f;
        direction = 0.f;
    }
    Vector(float speed, int direction)
    {
        this->speed = x;
        this->direction = direction;
    }







private:


};
